# Redis (required) — provisioned via Vercel Marketplace
REDIS_URL=

# Honeycomb tracing (optional) — omit to disable tracing
HONEYCOMB_API_KEY=
HONEYCOMB_SERVER=https://api.honeycomb.io  # EU: https://api.eu1.honeycomb.io

# Local development
PORT=3000
